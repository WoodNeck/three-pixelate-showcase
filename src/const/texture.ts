// TOP / SIDE texture size
export const SIZE = {
	TOP: {
		WIDTH: 16,
		HEIGHT: 16,
	},
	SIDE: {
		WIDTH: 16,
		HEIGHT: 8,
	},
};

// Interval between grid (black line)
export const GRID_INTERVAL = 4;

// Whether it's below half of the tile brick or not
export enum BRICK_FLOOR {
	TOP, BOTTOM,
}
